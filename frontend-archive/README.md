# Frontend Archive

Archived design explorations live here as a standalone sandbox app.

Run it with:

```bash
cd frontend-archive
npm install
npm run dev
```

Use the pill switcher on the right side to flip between the archived concepts.
